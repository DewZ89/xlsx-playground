{
  "name": "node-starter",
  "version": "0.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "fast-xlsx-reader": "^0.3.1",
    "xlsx": "^0.18.5"
  }
}
